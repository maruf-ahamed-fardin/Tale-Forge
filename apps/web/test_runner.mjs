import { getAIStatus, trainOnText, generateStoryAndChat, deleteTrainedStory } from './lib/story-engine.ts';

// We can test the TypeScript story engine directly via tsx/node or via basic assertions
console.log("Testing Story Engine logic directly...");
